# NPM Memory Optimization Settings
# Reduces memory usage during package installation

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Limit concurrent downloads
maxsockets=3

# Reduce cache size
cache-min=3600

# Disable audit during install (faster, less memory)
audit=false

# Disable fund messages
fund=false

# Use less memory for progress
progress=false
